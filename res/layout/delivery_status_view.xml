<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android"
       xmlns:tools="http://schemas.android.com/tools"
       android:orientation="horizontal">

    <TextView
        android:id="@+id/delivery_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginRight="4dp"
        android:layout_marginEnd="4dp"
        android:layout_gravity="center_vertical"
        android:textAppearance="@style/Signal.Text.Caption.MessageSent"
        android:textAllCaps="true" />

    <FrameLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:gravity="center">

        <FrameLayout android:id="@+id/pending_indicator_stub"
            android:layout_width="12dp"
            android:layout_height="12dp"
            tools:visibility="gone"/>

        <ImageView android:id="@+id/sent_indicator"
            android:layout_width="@dimen/delivery_status_diameter"
            android:layout_height="@dimen/delivery_status_diameter"
            android:src="@drawable/delivery_status_sent"
            android:visibility="gone"
            android:contentDescription="@string/conversation_item_sent__delivered_description" />

        <ImageView android:id="@+id/delivered_indicator"
            android:layout_width="@dimen/delivery_status_diameter"
            android:layout_height="@dimen/delivery_status_diameter"
            android:src="@drawable/delivery_status_delivered"
            android:visibility="gone"
            android:contentDescription="@string/conversation_item_sent__delivered_description"
            tools:visibility="gone"/>

        <ImageView android:id="@+id/read_indicator"
            android:layout_width="@dimen/delivery_status_diameter"
            android:layout_height="@dimen/delivery_status_diameter"
            android:src="@drawable/delivery_status_read"
            android:visibility="gone"
            android:contentDescription="@string/conversation_item_sent__message_read"
            tools:visibility="visible"/>

    </FrameLayout>


</merge>